<div>
  <h1>Seja bem vindo, {{nome}}!</h1>

  {{#if admin}}
    <h2>Item List</h2>
    <ul>
      {{#each items}}
        <li>{{nome}} - ${{preco}}</li>
      {{/each}}
    </ul>
  {{else}}
    <p>You don't have admin privileges to view the item list.</p>
  {{/if}}
</div>

{ O Handlebars é uma popular engine de templates usada em conjunto com o Node.js e o Express para criar páginas HTML dinâmicas no servidor. }
{ O modelo assume que você passará um objeto de dados para ele que contenha as propriedades "nome", "admin" e "items". }